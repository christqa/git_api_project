/**
 * The validity of a Token
 */
export enum Token {
  INVALID_TOKEN,
  VALID_TOKEN,
}

/**
 * The types of request methods
 */
export enum RequestMethod {
  POST = 'post',
  GET = 'get',
  DELETE = 'delete',
  PUT = 'put',
  PATCH = 'patch',
}
